name: Build CASE ZERO Android APK

on:
  push:
    branches: [ case-zero-full-game ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: yes | sdkmanager "platforms;android-36" "build-tools;35.0.0" "platform-tools" || true

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Apply bright isometric visual overhaul
        run: cd casezero_game && python3 tools/apply_bright_visuals.py

      - name: Generate lossless cozy mystery audio
        run: cd casezero_game && python3 tools/generate_casezero_audio.py

      - name: Build installable debug APK
        run: cd casezero_game && gradle --no-daemon --stacktrace :casezero:assembleDebug

      - name: Android 15 startup smoke test
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          disable-animations: true
          script: |
            adb install -r casezero_game/casezero/build/outputs/apk/debug/casezero-debug.apk
            adb logcat -c
            adb shell am start -W -n com.casezero.lastwitness.debug/com.casezero.lastwitness.MainActivity
            sleep 5
            adb logcat -d -v brief > startup-logcat.txt || true
            adb shell pidof com.casezero.lastwitness.debug | grep -Eq '[0-9]+'
            adb exec-out screencap -p > casezero-v2-startup.png

      - name: Prepare APK artifact
        run: |
          mkdir -p release
          cp casezero_game/casezero/build/outputs/apk/debug/casezero-debug.apk release/CaseZero-TheLastWitness-v2.0-Bright.apk
          ls -lh release/CaseZero-TheLastWitness-v2.0-Bright.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: CaseZero-TheLastWitness-v2.0-Bright-APK
          path: release/CaseZero-TheLastWitness-v2.0-Bright.apk
          if-no-files-found: error
          compression-level: 0

      - name: Upload startup diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CaseZero-v2-startup-diagnostics
          path: |
            startup-logcat.txt
            casezero-v2-startup.png
          if-no-files-found: ignore
